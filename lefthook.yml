pre-commit:
  commands:
    lint:
      run: pnpm lint
    format:
      run: pnpm format:check
    type-check:
      run: pnpm type-check

commit-msg:
  commands:
    commitlint:
      run: pnpm commitlint --edit {1}
